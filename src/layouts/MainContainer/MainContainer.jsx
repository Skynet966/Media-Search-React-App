import SearchBar from '../../components/SearchBar/SearchBar';
const MainContainer = () => {
	return (
		<div>
			<div className=''>
				<SearchBar />
			</div>
			<section>
                Result Container
            </section>
		</div>
	);
};

export default MainContainer;
